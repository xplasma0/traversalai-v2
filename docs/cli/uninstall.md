---
summary: "CLI reference for `traversalai uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `traversalai uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
traversalai uninstall
traversalai uninstall --all --yes
traversalai uninstall --dry-run
```
